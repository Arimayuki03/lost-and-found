# 安全政策

## 支持的版本

本项目目前处于初始开源阶段，仅对最新主分支提供安全修复：

| 版本 | 支持情况 |
| --- | --- |
| 最新 main 分支 | ✅ 支持 |
| 历史提交 / 旧标签 | ❌ 不支持 |

## 如何报告安全漏洞

**请勿在公开 Issue、PR 或讨论区中披露安全漏洞。**

请通过以下方式私下报告：

- 在 GitHub 上通过仓库页面的 **Security → Report a vulnerability**（私密漏洞报告）提交；或在 Issue 中留言请求维护者提供私下联系方式。

报告时请尽量包含：

- 漏洞类型与影响的接口/模块；
- 复现步骤或概念验证（PoC），注意不要包含真实密钥与真实个人数据；
- 影响评估与（如有的话）修复建议。

## 响应时限承诺

- **确认收到**：报告提交后 **72 小时内**；
- **初步评估**（严重程度与影响范围）：**7 天内**；
- **修复发布**：高危漏洞力争 **30 天内**发布修复，中低危随常规版本发布；
- **公开披露**：在修复发布并与报告者协商后统一披露，报告者将被致谢（除非要求匿名）。

## 不支持的内容

以下内容不在安全政策支持范围内：

- 对**默认/弱口令**、未修改的示例密钥等自身部署不当造成的问题（请先修改 `.env` 中的默认值与密钥）；
- 未在受支持版本列表内的旧版本；
- 需要物理访问或本地管理员权限才能利用的攻击；
- 社会工程、拒绝服务（DoS/DDoS）等不依赖代码缺陷的攻击；
- 针对第三方服务（MySQL、Redis、MinIO、腾讯云、SMTP 等）自身的漏洞，请向相应厂商报告。

## 安全设计速览

供安全研究者参考，代码中的关键安全机制：

- JWT 双令牌（access 15 分钟 / refresh 30 天），密码修改后旧令牌自动撤销（`app/utils/token_revocation.py`）；
- 登录失败锁定（三入口独立计数，`app/utils/account_lock.py`）与 Redis 接口限流（`app/utils/ratelimit.py`）；
- 密码 werkzeug 哈希存储，历史明文登录时自动升级（`app/utils/password.py`）；
- 日志 SQL 绑定参数脱敏（`app/utils/log_sanitize.py`）；
- 上传图片不良内容检测（腾讯云 TIIA）与体积/格式限制。
